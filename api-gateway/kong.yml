# Kong Declarative Configuration — DB-less mode
# Minimal config for Phase 1: passthrough to Orchestrator
# Phase 8: Add JWT plugin, CORS plugin, rate limiting, full route definitions

_format_version: "3.0"

services:
  - name: orchestrator
    url: http://orchestrator-service:5003
    routes:
      - name: orchestrator-health
        paths:
          - /health
        strip_path: false
