# Monorepo Layout

Each subdirectory here is a placeholder for a self-contained Flask service.
During implementation, add each service's `app.py`, `models.py`, `routes.py`,
`requirements.txt`, `Dockerfile`, and `migrations/` here.

This placeholder README can be deleted once real implementation files are added to this folder.

