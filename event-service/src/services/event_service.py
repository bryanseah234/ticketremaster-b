"""
Event Service — Event Service
Handles event listing, detail with seat availability.
Phase 3: Implement GET /events and GET /events/{event_id}.
Calls Inventory Service HTTP sidecar for seat availability (choreography).
"""
