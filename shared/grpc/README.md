# Shared gRPC Stubs

These files are generated from `proto/seat_inventory.proto` using `grpc_tools.protoc`.
Copy both generated files into any service or orchestrator that needs the Seat Inventory
gRPC client or server implementation, keeping them in the same directory.
