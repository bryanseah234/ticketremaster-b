apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-portforward-to-kong
  namespace: ticketremaster-edge
spec:
  podSelector:
    matchLabels:
      app: kong
  policyTypes:
    - Ingress
  ingress:
    - {}
