# Orchestrator Layout

Each subdirectory here is a placeholder for a self-contained Flask orchestrator.
Add each orchestrator's `app.py`, `routes.py`, `middleware.py`,
`requirements.txt`, and `Dockerfile` as you build each phase.

This placeholder README can be deleted once real implementation files are added to this folder.

